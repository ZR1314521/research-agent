---
name: workflow-visualizer-dashboard
description: Render current session steps, skill calls, status, artifacts, errors, and pending decisions as terminal text and structured JSON. Use for progress and workflow inspection.
---

# Workflow Visualizer

Read session events without changing them. Show chronological status in the terminal and write `workflow_trace.json` for later UI use. This development version does not require a web dashboard.

