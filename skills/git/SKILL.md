---
name: git
description: Version control for research artifacts. Run git init, add, commit, log, diff, status, checkout, branch in the project or session directory. Use to track changes to papers, data, and analysis scripts.
---

# Git

Version control for research work. Auto-initializes a repo if .git does not exist. Pass the git command as a single string, e.g. "add -A", "commit -m 'save draft'", "log --oneline", "diff".
