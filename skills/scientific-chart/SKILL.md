---
name: scientific-chart
description: Render an explicit scientific chart specification from CSV, TSV, TXT, XLSX, or XLSM data and return a PNG or SVG artifact.
---

# Scientific Chart

Inspect the available dataset fields, then choose and call this tool only when a chart helps answer the user's request. Supply the chart operation and columns explicitly. Preserve source data, report unusable values, and do not claim a chart exists unless the returned artifact path is present.
