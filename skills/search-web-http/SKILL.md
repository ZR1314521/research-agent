---
name: web-search-http
description: Search the public web through a direct HTTP provider without OpenCLI or a browser.
---

# Direct HTTP web search

The model must select a supported provider explicitly. This route performs exactly that request and reports success, empty results, or failure without selecting a fallback route.
