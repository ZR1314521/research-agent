# Workspace files

Read, list, search, copy, or move files in the workspace. Works with text formats (MD, JSON, CSV, TXT). Reading DOCX files returns only the first portion of text (may not include the reference section at the end). Uploaded files are stored in the session's uploads directory — check latest_document artifact for the exact path. Write/delete operations require confirmation.
