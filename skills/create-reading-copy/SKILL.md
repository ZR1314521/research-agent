# Open-access reading copy

Create a DOCX reading copy from previously downloaded open-access PDFs. Requires PDFs downloaded by open-access-paper-download first. Extracted text only, never the publisher's original Word document.
